- baseInfo:
    api_name: 白名单删除
    url: /park-access-parking-rule-new/mock/whitelist/delete
    method: post
    header:
      Content-Type: application/json;charset=UTF-8
  testCase:
    - case_name: 白名单删除
      json:
        codes: ${get_extract_data_list(carCodes, 0)}
      validation:
        - eq: {'success': 'true'}